name: Build and Push Multiple to GHCR

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # Wajib untuk push ke GHCR

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          ghcr.io/${{ github.repository_owner }}/brojol:latest
          ghcr.io/${{ github.repository_owner }}/jokoway:latest
          ghcr.io/${{ github.repository_owner }}/praminis:latest
          ghcr.io/${{ github.repository_owner }}/suisei:latest
          ghcr.io/${{ github.repository_owner }}/tenxi:latest
